spring:
  application:
    name: hr-user

  cloud:
    config:
      uri: http://localhost:8888
      
  profile:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"