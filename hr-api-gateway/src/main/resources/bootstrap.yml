spring:
  application:
    name: hr-api-gateway

  cloud:
    config:
      uri: http://localhost:8888
      
  profile:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"